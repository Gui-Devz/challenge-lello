import styled from "styled-components";

export const Container = styled.div`
  img {
    cursor: pointer;
    width: 45px;
    border-radius: 50%;
  }

  p {
    cursor: pointer;
    margin: 0;
  }
`;
